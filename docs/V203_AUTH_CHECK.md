# v2.0.3 auth verification

- Registration UI: email + password + confirm password + recommendation code.
- Current recommendation code: `JY888`.
- Registration calls `invite-register`; it does not call client-side `auth.signUp`.
- Edge Function creates an email-confirmed account and the client signs in immediately.
- Invalid recommendation code HTTP smoke test: passed.
- `JY888` registration + password login HTTP smoke test: passed.
- Smoke account removed and invite usage restored after test.
- Auth hero uses the generated raster watercolor landscape embedded as optimized WebP, cropped for the mobile hero region.
- Android version: `2.0.3-beta`, versionCode `3`.
