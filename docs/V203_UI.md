Auth UI label is `推荐码注册`; recommendation-code input is highlighted and provides `JY888` as an example. The raster hero is positioned with `object-fit: cover` and a mobile-specific crop/gradient so text remains legible without reverting to geometric SVG shapes.
